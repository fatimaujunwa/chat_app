import 'package:flutter/cupertino.dart';

class SignUpSignInScreenTemplate extends StatelessWidget {
  const SignUpSignInScreenTemplate({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(

    );
  }
}
